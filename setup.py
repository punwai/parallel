from setuptools import setup, find_packages

setup(
    name="parallel",
    version="0.1.0", 
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.8",
    description="Parallel reasoning experiments with language models",
    author="punwai",
    author_email="",
    package_dir={"": "."},
)
